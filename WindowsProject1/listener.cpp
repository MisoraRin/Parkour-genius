#include"stdafx.h"
#include"listener.h"
//bool listener::death_listener(graphic &b, vector<barrier>&a) {
//	for (int i = 0; i < (int)a.size; i++) {
//		if (a[i].x_get < 50)
//			if (b.y_get - a[i].height_get < 40);
//				//game_over();//程序结束函数，没写呢
//	}
//} 